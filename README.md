Provider Validation Drupal module
-------------------------------------

* CustomUrlChecking (validates fields): test for only letters, numbers, undercores and dashes, unique case-sensitive values, unique path alias.